PORT=5000
CORS_ORIGIN=http://localhost:3000

# Option 1: use a single MySQL connection string from your host
# DATABASE_URL=mysql://username:password@hostname:3306/database_name

# Option 2: use individual variables
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=chota_beta

# Set to true only if your MySQL provider requires SSL
DB_SSL=false

DB_CONNECTION_LIMIT=10
JWT_SECRET=change-me-mobile-secret
JWT_EXPIRES_IN=7d
